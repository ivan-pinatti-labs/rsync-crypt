[pytest]
testpaths = .
# conftest.py is imported directly by the test modules, so its directory has to
# be on sys.path. rootdir insertion gives that without an installable package.
addopts = -ra --strict-markers
markers =
    scripts: no containers or stack state, exercises a script under scripts/ directly
